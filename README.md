# Crypto_Analysis

An automated weekly analysis toolkit for **Bitcoin (BTC)** — calculates long-term market indicators, generates charts, and produces a **Word report** with metrics and interpretations for investment insights.

---

## 🧭 Objective

Perform a **weekly Bitcoin market analysis** and automatically generate a **Word (.docx) report** with graphical summaries, metrics, and indicator-based evaluations of market conditions.

---

## 📊 Key Indicators Calculated

- **2-Year SMA** (`window=730`) and **2-Year MA ×5**
- **DMA (Displaced Moving Average)**: shifted versions of the 2-Year SMA and 5×2-Year MA  
- **Mayer Multiple**: `Close / 200DMA` with reference lines at:
  - **2.4×** → Overheated (potential market top)  
  - **0.75×** / **0.65×** → Undervalued (accumulation zones)
- Additional scoring logic for **undervalued/overvalued zones**, color-coded in plots and report tables

---

## 📤 Outputs

- 📈 PNG charts showing BTC price vs. indicators  
- 📄 Word report (.docx) with:
  - Summary and interpretation
  - Visual indicators
  - Zone-based signal tables (e.g., accumulation/overbought levels)

---

## 💡 Utility

Provides a **clean, consolidated view** of Bitcoin’s market state, allowing long-term investors to evaluate when BTC is historically overvalued or undervalued using key metrics such as the **Mayer Multiple** and **2-Year averages**.

---

## ⚙️ How to Use

1. Place your historical BTC data (e.g.  
   `BTC_EUR Kraken Historical Data.csv`) in the `data/` folder.
2. Open the notebook in **Jupyter Notebook or JupyterLab**.
3. Execute all cells in order.
4. The notebook will automatically generate plots and export the report under `/reports/`.

---

## 🧱 Project Structure
```
.
├─ data/ # Source data (CSV format)
│ └─ BTC_EUR Kraken Historical Data.csv
├─ notebooks/
│ └─ BTC Weekly Report @keepitcrypto.ipynb
├─ outputs/
│ └─ charts/ # Generated PNG charts
├─ reports/
│ └─ BTC_Weekly_Report.docx # Word report generated by the notebook
├─ README.md # This file :)
├─ .gitignore # Ignore data/output files
└─ requirements.txt # Python dependencies
```
yaml
Copy code

---

## 🧰 Dependencies

- `pandas`  
- `matplotlib`  
- `numpy`  
- `pandas_ta` *(optional – for additional indicators)*  
- `python-docx` *(for exporting Word reports)*  
- Standard libraries (`datetime`, `os`, etc.)

---

## ⚡ Quick Start

### 1) Prerequisites
- Python 3.7 or higher  
- Git installed  
- (Recommended) virtualenv/venv or conda

### 2) Clone & install

```bash
git clone <REPO_URL> crypto-analysis
cd crypto-analysis

# Create and activate a virtual environment
python -m venv .venv
# Windows:
.venv\Scripts\activate
# macOS/Linux:
source .venv/bin/activate

# Install dependencies
pip install --upgrade pip
pip install -r requirements.txt
🧾 Viewing Results
Each run creates a report file under /reports/, for example:

yaml
Copy code
reports/
└─ BTC_Weekly_Report - 2025-11-10.docx
and chart outputs under /outputs/.

📦 Data Sources
You can download historical data manually from:

Bitcoin Historical Data (Investing.com)

Place the file inside data/ as a .csv file.

🧑‍💻 Git Workflow
Follow a simple trunk-based flow with short feature branches and Conventional Commits.

Branch Naming
Use kebab-case with prefixes like:

feat/ — new features

fix/ — bug fixes

docs/ — documentation

refactor/ — code cleanup

Examples

bash
Copy code
feat/add-mayer-multiple-analysis
docs/update-readme
fix/report-export-bug
Conventional Commits
Format:

php-template
Copy code
<type>(<scope>): <short summary>
Examples:

scss
Copy code
feat(report): add automatic Word export with charts
fix(data): handle missing values in historical dataset
